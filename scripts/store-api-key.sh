#!/usr/bin/env bash
set -euo pipefail

echo "Store or replace the API key through OpenDictate's API-key dialog." >&2
echo "This helper no longer writes Keychain entries. No key was stored." >&2
exit 1
